# Claim corrections

Commit messages on this branch are load-bearing evidence for reviewers and the
owner. When a message claims more, less, or something different from what its
commit establishes, that mismatch is a defect of the same class being removed:
a claim unsupported by what it describes. Landed commits are not rewritten;
this record preserves the correction and its evidence.

| Commit | Claim as written | What is actually true | Evidence re-derived | Category |
| --- | --- | --- | --- | --- |
| `d29cf4bc` | "235 passed across tests/web, tests/test_rest_api.py and tests/test_web_dashboard.py, plus tests/core/test_memory_leaks.py (12 passed)." | The complete cited set was **247 passed**: 235 in the first three partitions and 12 in the memory-leak partition. Presenting 235 as the cited-set figure omitted the stated final partition. | At the landed tree, the three non-web partitions contain 196, 18, and 12 passing cases. The historical web partition contains 21, for 21 + 196 + 18 + 12 = 247. The current separate runs report 26, 196, 18, and 12; the five-case difference is from the later edit to the dashboard-unavailability contract test. | **FALSE** |
| `b1ad7ca0` | "Proven by negative case, twice and independently," including rendering the unavailable typed results. | The availability-rendering red-before is not behavioural proof. The parent `CooldownETA` rejects the newly introduced `available=` keyword with `TypeError`; other new rendering cases dereference newly introduced `.available` on old `None` results. Those failures are caused by symbols and fields added by this commit. The distinct value-level adapter cases that establish that an old failed reply is `None` where the new contract requires a typed unavailable result remain valid behavioural evidence. | The new rendering test constructs all result types with `available=False`, while the parent schemas lack those fields; the parent adapter returns `None` on failed replies. The same distinction is disclosed correctly by `ef022ab5`: it says its old `test_agent_dedup.py` red is an `AttributeError` and explicitly excludes it as proof, retaining its alarm-flow evidence instead. | **FALSE** |
| `3d6d1a22` | The broadened C2 guard is presented as the new seal without disclosing an exemption introduced with it. | The commit added a real C2 exemption for `src/cryodaq/reporting/periodic_renderer.py` at line 142, for thermometry-style sorting, while the guard comment still said C2 had no exemptions. `5354fb9c` is not implicated: its allowlist was empty when it landed; this exemption was added later. | The parent C2 guard has an empty `_ALLOWLIST`. This commit adds the `(periodic_renderer, 142)` `BLOCKED-ON-SCHEMA` entry, and the renderer's `_channel_key` performs the matched channel-name ordering. | **UNDISCLOSED** |
| `b1ad7ca0` | "927 passed, 2 skipped across tests/agents." | This was a historical measurement, not a false claim. It cannot be reproduced as a comparable count on this tree and host. | Later commits changed several tests in the partition, including the C1 seal and alarm-delivery tests. A separate current run reports 924 passed, 3 skipped, 1 deselected, and 7 failures; every failure is `WinError 1314` while creating a symlink. This host lacks the symlink privilege available to CI. | **STALE** |
| `ef022ab5` | "932 passed, 2 skipped across tests/agents." | This was a historical measurement, not a false claim. It cannot be reproduced as a comparable count on this tree and host. | The same later test changes and current host limitation apply. The current separate `tests/agents` run reaches 924 passed, 3 skipped, 1 deselected, and 7 `WinError 1314` symlink-creation failures, so it is not evidence against the landed count. | **STALE** |
| `5354fb9c` | "508 passed, 12 skipped across tests/analytics and tests/reporting." | This was a historical measurement, not a false claim. It is not comparable to the current tree. | The later C2 broadening changed the analytics guard test included in this cited set. A current analytics-only rerun was stopped by the session command window before completion; it must not be represented as a rerun of the landed count. The host also lacks the symlink privilege that CI provides, independently demonstrated by the current agents partition. | **STALE** |
| `3d6d1a22` (second and third omissions) | "Broaden two guards that asserted a class while proving one shape." | Besides the C2 allowlist entry recorded above, the same commit **narrowed** `_REGEX_METHODS` from six entries to three, and changed the C1 adapter guard's clean baseline from requiring zero findings to accepting `_KNOWN_PRODUCTION_VIOLATIONS` — specifically `archive_adapter.py:get_detail:228`. A commit titled as a broadening performed three separate narrowings, none disclosed. | `tests/agents/assistant/test_c1_engine_adapter_seal.py:31-36` declares the accepted violation and line 228 asserts equality with that set rather than emptiness. The C2 guard's direct-regex method set is three entries where the parent had six. | **UNDISCLOSED** |
| `1d2c43ad` | Presented as a G4 documentation-guard improvement. | It **relaxed** G4: it deleted the unconditional rejection of `SOFTWARE-PROVABLE` procedure declarations and added a test asserting the new acceptance. The relaxation is defensible — a genuinely software-provable procedure previously could not be expressed — but the message did not disclose that a guard stopped rejecting a class it had rejected. | `tests/docs/test_docs_freshness.py:834` now accepts `{"SOFTWARE-PROVABLE", "EXTERNALLY_EVIDENCED", "PHYSICAL"}`. The parent rejected the first unconditionally. | **UNDISCLOSED** |
| `dcdb1912` | G4 rejects a procedure claiming `SOFTWARE-PROVABLE` where the evidence is external or physical. | True in its own diff, and no longer true: `1d2c43ad` deleted that rejection. | Same evidence as the row above. | **STALE** |
| `0f505dd6` | "A driver-level OFF double that returns anything other than a `SourceOffResult` fails CI." | **The guard is not universal.** It exempts five named scopes and skips every `emergency_off` annotated `dict[...]`. Several of those exemptions are legitimate — `test_truthy_non_boolean_proof_cannot_authorize_disconnect` is a negative control that must be able to pass a truthy non-`SourceOffResult` — so the guard's design is sound. The absolute claim was not. | `tests/governance/test_source_off_result_test_doubles.py:10-16` lists `_INTENTIONAL_INVALID_SCOPES`; lines 124-131 skip exempted scopes and `dict[...]`-annotated returns. | **FALSE** |
| `a511aa1d` | "Guarded by `tests/core/test_experiment_archive_descriptors.py` (7 nodes)"; "327 passed"; "FIXED POINT, tree `0125d1cb`, 1,206 files"; and that OC-024 "leaves the pinned BLOCKS-DEPLOYMENT inventory". | **Every one of those four is now false of this branch, and the last is the one that matters.** Review showed the 7-node module guards the WRITER only -- reverting the entire loader carry leaves all 7 GREEN -- so a second module was added and the guarded set is 16 nodes. `tests/docs` + `tests/governance` is 330. The derived snapshot is a different object over 1,207 files. And OC-024 **stays** `DEFECT` / BLOCKS-DEPLOYMENT: the four sibling exporters reachable from the same operator-facing archive panel were never measured, so deregistering the row closed a deployment gate on the strength of a prose residual. | **PENDING IMMUTABLE CAPTURE, deliberately: these figures are measured against the branch head that carries this row, and writing that head's identifier into this file changes the file and therefore the head.** They are bound MECHANICALLY instead, by commands that re-derive them at whatever head a reader is on: `python -m pytest --collect-only -q tests/core/test_experiment_archive_descriptors.py tests/core/test_experiment_archive_loader_identity.py` for the node count; `python -m pytest tests/docs tests/governance -q` for the pass count; for the snapshot and its file count, `python -m pytest tests/docs/test_docs_freshness.py -q`, whose `test_checked_in_montana_architecture_svgs_match_frozen_index_snapshot` and `test_montana_report_inventory_metrics_match_frozen_index_snapshot` regenerate from the STAGED INDEX and fail on any drift -- the regeneration itself is driven by the tracked `tools/generate_montana_architecture_svgs.py`. An earlier version of this row named a path under `C:/tmp`, which exists only on the author's machine: a command a reader cannot run is not a mechanical binding, it is a claim wearing one; and `tests/docs/test_docs_freshness.py` failing if `OC-024` leaves the pinned BLOCKS-DEPLOYMENT inventory. If any command disagrees with a figure above, the figure is stale and this row is the thing to correct. | **FALSE at that head** |

| `14b2c432` | "Make the disclosure register true at the commit it ships with." Message describes documentation only. | The commit also carried 59 lines of `tests/core/test_safety_operator_snapshot_owner.py` -- tier test expectations whose production code was not yet landed. At the resulting pushed head that file **hangs**: the double is classified `COMMAND_ONLY`, SafetyManager correctly faults closed and never disconnects, and the test waits forever for a disconnect that should not happen. So the commit that claims to close the honesty gap silently broke `tests/core` and invalidated verification claims made after it. | Reproduced at `e642cba4`: the file times out. The tier production code landed later, in `757f4310`, after which the same file passes. | **UNDISCLOSED** |
| `14b2c432` (OC-013) | The register was rewritten to say the OFF evidence tiers "no longer collapse" and are "preserved through SafetyManager -> scheduler -> operator snapshot -> engine receipt -> launcher". | True of a patch that was **not committed at that commit**. The register described unlanded work as landed, in the document whose entire purpose is to be true at the commit it ships with. It became true at `757f4310`. | The tier production change is absent from `git show 14b2c432` and present in `757f4310`. | **FALSE at that head** |
| `6f2ef69a` | "Fold the GUI identifier sites into the one C2 registry." Message states no file under `src/cryodaq/gui/` is modified and describes only the registry. | The commit also carried the entire OC-006 change -- `src/cryodaq/core/safety_manager.py`, `tests/core/test_keithley_channel_state_publish.py` and `tests/gui/shell/overlays/test_keithley_panel.py` -- which is a safety-path publication change, not a registry change. | `git show --stat 6f2ef69a` lists four files; the message accounts for one. | **UNDISCLOSED** |
| `4520d6ac` | "The acceptance control is deterministic and synthetic. A real hosted green run" had never been accepted by the partition-execution verifier. | **True when it landed, and superseded.** The verifier has now accepted a real hosted run: `30419591227` at `eecadd51`, eight partitions, each with a non-zero collected AND executed count. The reject-only gap that commit disclosed is closed. *** It does NOT follow that merge property (b) is established: that acceptance was produced by a verifier running from the CANDIDATE, which is precisely OC-038 — the protected `workflow_run` chain has never executed because the workflow is absent from the default branch. So this is candidate-workflow evidence, not the protected acceptance receipt. *** | The acceptance was only possible after `390e4a29` taught production to publish the population receipts the verifier reads; before it, the `ubuntu/agents` job log carried zero `CRYODAQ_*` markers in 143,302 bytes while the artifact carried two. Measured after: two in the log, matching. | **STALE** |
| `035eba12` (documentation snapshot; OC-021) | `docs/OPEN_CELLS.md` marked OC-021 **CLOSED** and said it was "Verified independently at the frozen head under P7." | The protected hosted chain did not execute in Cycle 1. The local validator/regressions are authored software evidence, not the missing independent hosted receipt. OC-021 remains open until Cycle 2 independent review, exact candidate binding, and protected hosted P7 execution succeed. | The same register's retained Cycle 1 history says at lines 206-212 that the protected chain had not executed; OC-035, OC-038, `PROJECT_STATUS.md`, and the owner-ratified Cycle 2 boundary all keep independent review and hosted proof pending. | **STALE** |
| `035eba12` (documentation snapshot; OC-033) | `docs/OPEN_CELLS.md` marked OC-033 **CLOSED** and said it was "Verified independently at the frozen head under P7." | The critical-input identity correction has local behavioral support and ordinary hosted `core` coverage, but no Cycle 2 frozen candidate or P7 evidence exists. OC-033 is therefore implemented with Cycle 2 evidence open, not independently verified or checkpoint-closed. | `PYTHONPATH=C:\tmp\montana-pra\src python -m pytest -q tests/core/test_safety_critical_input_identity.py` passes all 11 nodes as an unfrozen current-worktree diagnostic, not reusable acceptance evidence. Ordinary hosted run `30414396538` at `94ad6812` ran both `core` jobs green, but the overall workflow was red and it was not protected P7 evidence. P5 freeze, both P6 final-review receipts, and both exact-SHA P7 run receipts remain required. | **FALSE** |
| `035eba12` (documentation snapshot; OC-034 test count) | `docs/OPEN_CELLS.md` cited `tests/test_artifact_promotion.py` as "44 passed, 1 skipped" without labelling the count historical or binding it to an immutable object. | The count was an unbound historical observation, not current evidence. The exact current test source contains ten top-level tests, while OC-034 remains open until the repository ruleset and direct-release restriction are established at P9. | Immutable source blob `1c1256e5c8274186f61214fa433d31b6053bc62d` contains ten top-level `test_*` functions. The staged-index freshness guard derives that count; no immutable P5 candidate manifest currently binds a final commit/tree, so that binding remains pending until independently verified. | **STALE** |
| `035eba12` (documentation snapshot; physical T11/T12 claims) | Config comments and historical release prose called the T11/T12 stand mapping lab-verified, physically bolted/fixed, calibrated, and "Физически точно." | The tracked files declare T11/T12 roles and software aliases only. Physical attachment, role confirmation, and calibration acceptance remain open under `docs/lab_verification_checklist.md`; no parsed config value was changed by this correction. | `PROJECT_STATUS.md` explicitly calls the mapping an open gate and forbids RUN authority or an accepted UI/report/alarm mapping before an exact-descriptor binding receipt. | **FALSE** |
| `035eba12` (documentation snapshot; Montana branch identity) | Both READMEs named `feat/montana-phase-a` as the active candidate branch. | That branch label was stale campaign routing, not a current acceptance object. The READMEs now name Montana Cycle 2 without inventing a branch or SHA and defer the live boundary to `PROJECT_STATUS.md`. | The owner-authorized Cycle 2 boundary in `PROJECT_STATUS.md` and `docs/OPEN_CELLS.md` supersedes the earlier candidate wording and intentionally withholds a successor judge SHA. | **STALE** |
| `035eba12` (documentation snapshot; platform execution) | `README.ru.md` said the candidate "проверяется" on exact-SHA Windows, WSL/Linux, ONEDIR, and hosted CI, which reads as current execution. | Those are required procedures, not completed Cycle 2 results. The exact candidate, protected hosted P7, Windows/WSL, and ONEDIR gates remain pending unless their prescribed receipts are recorded. | `PROJECT_STATUS.md` and both README acceptance-boundary sections list those gates as future requirements and keep physical/packaging/platform acceptance open. | **FALSE** |
| `035eba12` (documentation snapshot; laboratory-ready labels) | Current overview and historical changelog language called CryoDAQ a system that "runs a cryogenics laboratory" and called v0.15.0 ready for the laboratory / production. | CryoDAQ is laboratory acquisition/control/operator-support software. Historical release labels are publication history, not physical, dummy-load, final-element, or operator-acceptance receipts. | `PROJECT_STATUS.md` keeps the release boundary at v0.64.1 and expressly says Montana does not establish release readiness, real-instrument, packaged-Windows, or laboratory acceptance. | **UNSUPPORTED** |
| `035eba12` (documentation snapshot; unavailable-state universality) | README and Unreleased changelog prose generalized named screenshot/fix behavior into claims that unavailable/stale data never appears as zero/current/green. | Named panels and repaired paths have conservative unavailable behavior; no product-wide inventory or production-path test proves that every GUI view revokes a dead producer's retained value. | OC-004 records the missing served-state/producer-invalidation inventory and per-view regressions; the screenshots demonstrate only their rendered paths. | **FALSE** |
| `035eba12` (documentation snapshot; cross-device identity) | `README.md` said one device's data cannot be mistaken for another's merely because labels match. | Exact descriptors/bindings protect named paths, but multiple GUI, archive, interlock, assistant, and notification consumers still use spelling or lack descriptor authority. | OC-008, OC-011, and OC-023–OC-025 enumerate the open identity/descriptor paths. | **FALSE** |
| `035eba12` (documentation snapshot; source capability authority) | `README.md` said hazardous source authority is capability-gated. | Current generic source authority is restricted by a one-item `keithley_2604b` roster and driver-type equality. Capability-derived authority remains open. | OC-007 records the production roster/type checks and the required `instruments.yaml` capability-derived replacement. | **FALSE** |
| `035eba12` (documentation snapshot; descriptor propagation) | README and Unreleased changelog prose claimed canonical descriptors run end to end from acquisition/SQLite through archive, replay, reports, and GUI. | Descriptor propagation exists on named paths, not end to end. Archive-finalisation rows lack descriptor data and several GUI/interlock/assistant/notification paths still lack authority. | OC-008, OC-011, and OC-023–OC-025 identify the remaining consumers and closure gates. | **FALSE** |
| `035eba12` (documentation snapshot; assistant boundary) | Both READMEs described the running operator-query service as read-only, non-mutating, and without engine-command access. | The intended query adapter boundary is read-only, but the current service still holds a second operator-log writer, a RAG mutation path, and a Telegram credential. The whole assistant is not yet observational/read-only. | The README architecture section, `PROJECT_STATUS.md`, and the Montana contract all list those write/credential paths as open removal or ownership work. | **FALSE** |
| `035eba12` (documentation snapshot; physical roster heading) | `PROJECT_STATUS.md` presented the configured device table under the unqualified heading "Физическая установка." | The table is the v0.64.1 declared/configured roster, not evidence that the devices, interfaces, and channel bindings were physically verified on the current stand. | The immediately following mapping paragraph and `docs/lab_verification_checklist.md` keep physical verification open. | **UNSUPPORTED** |
| `035eba12` (documentation snapshot; default-branch distance) | `PROJECT_STATUS.md` and `docs/OPEN_CELLS.md` asserted as current that `master` was exactly four commits beyond `f5d6434d`. | The count described a planning snapshot but was not bound in those documents to an immutable default-branch object; it cannot serve as current evidence after later branch movement. | Both documents already withhold a successor Cycle 2 judge SHA and require a fresh reviewed/pinned object. | **STALE** |
| `035eba12` (documentation snapshot; Ruff counts) | `AGENTS.md` gave exact repo-wide and changed-file Ruff results for a "clean head" / "then-current head" without recording the immutable commit or tree. | Those observations are historical context only and cannot certify another head. Agents must rerun the workflow-exact changed-file gate and bind any evidence receipt to the exact object. | `AGENTS.md` itself requires tracked current counts and CI claims to cite an immutable commit/tree/blob or be labelled historical/pending. | **UNSUPPORTED** |
| `035eba12` (documentation snapshot; v0.64 audit closure) | The v0.64.0 changelog title said "fail-closed до конца" and the introduction described a whole-repository audit whose defects were all closed. | The listed release-slice findings were corrected; the wording did not establish complete repository review or a finished fail-closed property. Later Montana review found additional open safety, identity, evidence, and lifecycle classes. | `docs/OPEN_CELLS.md` states that no whole-area, whole-defect-class cell qualifies as sealed and lists the remaining classes. | **FALSE** |
| `035eba12` (documentation snapshot; historical release absolutes) | Historical changelog entries used unqualified "verified against real instruments," "if data are visible they are already on disk," and "all 10 GUI tabs fully functional." | The real-instrument sentence retained no object/protocol receipt; persistence-first covered the named archive-selected `DataBroker` path rather than every operator-visible surface; and "fully functional" meant wired/available in that release slice, not complete operability or laboratory acceptance. | Current README/`PROJECT_STATUS.md` distinguish archive-selected publication from raw/synthetic paths, keep GUI scenario coverage open, and require prescribed physical evidence. | **UNSUPPORTED** |
| `15e5fd5f` (ROADMAP trust-root pin) | P0 said the default branch was four commits after `f5d6434d`; P1 pinned `ccf15129` and a twelve-file range; P2/P4/P5/P7/P8/P9 continued to use `ccf15129` as the judge/base authority. | The current Cycle 2 judge is commit `3656654d00937230390076bc60a72b279c124aa9`, tree `2bd5e59f73c0326b2a740f7e8d731e390b2a511c`, exactly eight commits after `f5d6434d20dffae62c9f03fbc12f68b03f48351b`, with fourteen changed trust-root paths. `ccf15129` was superseded by the next judge push. This object correction supplies no P1 completion evidence. | The repository comparison `f5d6434d20dffae62c9f03fbc12f68b03f48351b...3656654d00937230390076bc60a72b279c124aa9` reports `ahead_by: 8` and fourteen file entries. Reconstructing the target tree from the `9cc51caa3b404a8096948c830c6621d31871901a` judge index plus the seven later target blob IDs yields `2bd5e59f73c0326b2a740f7e8d731e390b2a511c`. | **STALE** |
| `15e5fd5f` (ROADMAP P2/P3/P5 phase table) | The adopted snapshot amendment said P2 remains uncommitted, P3 creates the sole `I`, and P5 sets `F := I` without committing; the phase table still said P2 creates `I` and P5 commits another `F`. | The amendment is the ruling. P2 prepares the merge with `--no-commit`; P3 stages all final content, regenerates the authorized snapshot from that final staged index, and creates the sole integration commit `I`; P5 sets `F := I` and creates no commit. | `ROADMAP.md`'s "Amendment — snapshot sequencing (P2-P5)" states the sequence explicitly. The corrected P2/P3/P5 rows now express the same sequence and leave P4 to verify the immutable `I`. | **FALSE** |
| `15e5fd5f` (`docs/OPEN_CELLS.md` successor state) | The register said the repaired Cycle 2 successor was pending, claimed no successor SHA, called the repair a working-tree change, and said the repaired commit and pin did not exist. | The repaired successor and pin exist at commit `3656654d00937230390076bc60a72b279c124aa9`, tree `2bd5e59f73c0326b2a740f7e8d731e390b2a511c`. The judge has exact object/inventory evidence and the authored mechanism; it still lacks both P1 review receipts, protected hosted evidence bound to that judge, and exact-candidate P7 evidence. | The same eight-commit/fourteen-path comparison and independently derived tree identify the current judge. No review or hosted receipt is cited because none exists. | **STALE** |
| `15e5fd5f` (`PROJECT_STATUS.md` successor state) | The status boundary said a repaired default-branch commit was pending and asserted no successor SHA. | The current successor is commit `3656654d00937230390076bc60a72b279c124aa9`, tree `2bd5e59f73c0326b2a740f7e8d731e390b2a511c`, eight commits after `f5d6434d20dffae62c9f03fbc12f68b03f48351b` across fourteen changed trust-root paths. P1 independent review and protected hosted proof remain open. | Repository object comparison and tree reconstruction establish identity and inventory only; `PROJECT_STATUS.md` continues to require the missing P1/P7 receipts. | **STALE** |
| `035eba12` correction row (Montana branch identity explanation, superseded at `15e5fd5f`) | Its correction said the current boundary intentionally withheld a successor judge SHA. | That explanation was true when written and is now superseded: the successor judge is pinned to `3656654d00937230390076bc60a72b279c124aa9` / `2bd5e59f73c0326b2a740f7e8d731e390b2a511c`. The branch-label correction remains valid; only the no-successor explanation became stale. | `ROADMAP.md`, `docs/OPEN_CELLS.md`, and `PROJECT_STATUS.md` now bind the same judge object while withholding all P1 completion claims. | **STALE** |
| `035eba12` correction row (default-branch distance explanation, superseded at `15e5fd5f`) | Its evidence said both plan surfaces withheld a successor SHA and required a future fresh reviewed/pinned object. | A fresh object is now pinned but not reviewed: `3656654d00937230390076bc60a72b279c124aa9`, tree `2bd5e59f73c0326b2a740f7e8d731e390b2a511c`, eight commits after the merge base with fourteen changed paths. The earlier unbound four-commit claim remains stale; the claim that no successor was pinned is newly stale. | The exact repository comparison and reconstructed tree establish the replacement counts and object. No P1 review receipts or protected hosted receipt exist. | **STALE** |
| `15e5fd5f` (changed-Python format count) | The commit message said the changed-file format gate was clean "across 159 changed Python files." | The workflow-exact changed-file set in the frozen PR7 staged index contains **672** Python paths; the prior snapshot's set contains **671** Python paths. This corrects the file count only; it does not independently reassert the commit message's clean-format result. | The freshness guard parses `FORMAT_BASE` from `.github/workflows/main.yml` and runs `git diff --cached --name-only --diff-filter=ACMR <FORMAT_BASE> -- '*.py'` against the frozen index. No immutable P5 candidate manifest currently binds the final commit/tree; that external binding remains pending until independently verified. | **FALSE** |
| `5fbf5b0e` (capability-derivation claim) | The round-1 commit message said capabilities are "DERIVED only from BUILTIN_DRIVER_SPECS" and its boundary-change list named no `src/` file. | True when written; superseded at `d0b80b41`. Review showed `BUILTIN_DRIVER_SPECS` is itself authority-bearing (public `factory` constructors), so derivation now reads only the inert `BUILTIN_DRIVER_METADATA` projection. The projection was added in `src/cryodaq/drivers/registry.py` at `d0b80b41` and its enums/metadata then moved to the deliberately authority-free `src/cryodaq/drivers/capability_metadata.py` after further review — upstream files the round-1 message did not name. | `d0b80b41` added `DriverTypeMetadata`/`BUILTIN_DRIVER_METADATA`; the later head moves the definitions to `capability_metadata.py` with the registry re-exporting them. `tests/lab_profile/test_downstream_readonly.py` proves the package never names `BUILTIN_DRIVER_SPECS` and that the projection is exact and factory-free. | **STALE** |
| `feat/lab-profile-v1` (changed-Python count, current candidate) | The freshness guard recomputes the workflow-exact changed-file set against the live candidate index, so the frozen-PR7 correction above cannot honestly carry the moving count. | The workflow-exact changed-file set in the current candidate index contains **690** Python paths. The frozen PR7 staged index correction stays pinned at **672**; this row carries the moving candidate count so later slices update it here rather than rewriting the historical correction. | The guard parses `FORMAT_BASE` from `.github/workflows/main.yml` and runs `git diff --cached --name-only --diff-filter=ACMR <FORMAT_BASE> -- '*.py'` against the live index; on this branch that set adds the nine lab-profile v1 Python paths plus the inert `src/cryodaq/drivers/capability_metadata.py` module to the frozen PR7 671. This is a **live, moving value and is PENDING IMMUTABLE CAPTURE** — no commit, tree or blob is cited for it, deliberately, because any such citation would be stale the moment it was written: the value is measured against the candidate *index*, and writing the identifier of that index into this file changes the index. It is therefore bound MECHANICALLY rather than by prose. `tests/docs/test_docs_freshness.py::test_claim_corrections_changed_python_count_matches_workflow_index` re-derives the count from the live index at every head and fails if this row disagrees, so the number here cannot silently describe a different object than the one being reviewed — which is the property a cited SHA would be trying to provide. History, for orientation only and not to be read as true of any other object: first recorded at commit `b2aef8b7c44186c9f8508475db85b817f6ba6a89`, re-derived to 681 when the inert module landed, to 682 when origin/master was merged into that branch, and on `fix/engine-loader-table-ownership` to 683 when `tests/core/test_yaml_loader_table_ownership.py` was added and to 684 when review surfaced a fourth affected loader and the owned loader moved into its own module, `src/cryodaq/_owned_yaml.py`. On `docs/phase1-oc031-truth` it went to 686 when a refusal harness for red-before controls was added, and back to 684 when that harness was moved out of this entry to its own branch as test infrastructure rather than register truth. On `fix/oc-024-archive-descriptors` it is 686, adding `tests/core/test_experiment_archive_descriptors.py` (the writer guard) and `tests/core/test_experiment_archive_loader_identity.py` (the loader guard review showed the first one could not see). On `fix/oc-028-narration-floor`, after taking master in by merge, it is 687 -- master now carries the two OC-024 guard modules and this branch adds one further changed Python path. On `feat/f36-4-health-telemetry` it is 689. After taking master in by merge it is 690: master is 689 after #24, #27 and #29 merged. That branch adds one new tracked Python path, `tests/drivers/test_health_telemetry_registry.py`. The row label names the branch that first carried it; the value it holds is the CURRENT candidate's, whatever branch that is. Any later slice that changes the Python changed-file set must re-derive the count here. | **UNDISCLOSED** |

## The mechanism behind those three, and the protocol change

All three have one cause: **`git apply --3way` stages what it applies.** Every
patch collected from a lane was therefore already in the index, and each
subsequent `git add <specific file>` followed by `git commit` swept up everything
staged, not just the file named. Explicit staging was being used, and it was not
sufficient, because the index was not empty when staging began.

Two of these were found by an independent reviewer and one by following the
mechanism the reviewer exposed. None was visible in the commit message, which is
the definition of the defect class this file records.

The protocol is now: **`git reset` immediately after every `git apply --3way`,
then stage explicitly.** Verify with `git status` that the index contains only
what the message describes, before committing.

## Prevention

Three commits above weakened or exempted a guard while their messages described a
strengthening. All three messages were written in good faith by an author who did
not notice, which is why the countermeasure cannot be prose.
`GUARD-COVERAGE-REGRESSION-034` records the mechanical form: a guard-coverage
inventory with stable challenge and exemption identifiers — never line numbers,
since line-number identity is precisely what let the `periodic_renderer` exemption
rot — compared across versions, with an exact reduction requiring a tracked
declaration rather than a trusted commit title. The check's acceptance test is
that it rejects `3d6d1a22` and `1d2c43ad` and names what each one lost.

## Coverage of this audit

An independent reviewer enumerated all 213 commits in the branch's review range
and read messages and patch hunks in full for 11 of them, selected as high-risk.
This table is therefore a sample of the highest-risk commits, not a complete
audit of 213 diffs, and must not be cited as one.
