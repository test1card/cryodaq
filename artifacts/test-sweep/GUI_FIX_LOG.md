# GUI FIX pass (batches 22-34) — live log

Strengthening the weak GUI tests surfaced by the FIND pass (batch-22..34-findings.md). Same
guardrails as the tier-0/1 fix pass: **TEST FILES ONLY, never src/**; defer anything needing a
prod/src change (mark DEFERRED, keep in the ledger); every touched file pytest-green + ruff-clean;
Qt tests use `QT_QPA_PLATFORM=offscreen`; independent re-run after each executor. The CRIT
(alarm-panel NaN, item 11) + all deferred-ledger items stay deferred. Machine state:
`progress.json` `gui_fix_next_batch`.

| Batch | findings | fixed | deferred | result | notes |
|------:|---------:|------:|---------:|--------|-------|
| 32 | 23 (7H/8M/8L) | 15 | 0 | 99 pass + Codex PASS | insight/steady-state/time-window/palette/theme/fonts (8 of 14 files; 6 clean incl. design-governance AST scan). SOURCE-GREP test_fonts → real `main()` invoke with `_load_bundled_fonts` stub (SystemExit-abort after the font call) — proves startup loads fonts. MOCK-BYPASS test_launcher_theme_switch (execv returns) → ordered call-log + `execv` raises SystemExit, full teardown sequence asserted. VALUE-BLIND steady-state → spy `_predictors[ch].add_point` (count+args, dup-ts guard) + curve/hero. insight-panel → rendered card label texts/trigger chip/`_count_label`"10/10"/newest-10 order. cooldown-trajectory → spy `set_prediction` exact (central/lower/upper/ci). palette → spy `app.setStyle("Fusion")`. noop-setters → seed real curve then immutable getData()-tuple snapshot. time-window → `findChildren(QPushButton)` labels+checked. **test_app_palette.py run ISOLATED (global-QSS gate caveat).** **Codex VERIFY amend cycle (gpt-5.5 high, in-batch): PASS no CRIT/HIGH; 2 MED amended — launcher order test now logs all 5 steps (bridge→engine→wait→release→execv) in one ordered list (was bridge/engine pair only; also kills a real 5s port-wait stall); noop snapshot upgraded from aliasing `dict(_curves)` to seeded immutable getData() tuples.** Re-verified palette isolated 7/0 + other 13 together 92/0 + ruff, src untouched. |
| 31 | 32 (22H/9M/1L) | 32 | 0 | 92 pass + Codex PASS | analytics widgets (5 files). NUMERIC-DISPLAY/WIDGET-CONTRACT/MOCK-BYPASS → exact rendered output: summary labels exact strings ("8.0 ч"/"0 (0 пред. / 0 крит.)"/"t_high ×3; pressure ×2; drift ×1"/full stat lines incl "ср 20.00"/full artifact paths); fp2-vacuum un-mocked set_history/set_prediction → real curve `getData()` in log10 space + CI band ±1σ; fp3-rthermal exact "0.120 К/Вт"+delta+curve, killed sigma-re-derive tautology → literal region values; trajectory private `_series` → curve getData() exact (xs/ys, sort, trim-to-5000, legend "Детектор"). 3/3 teeth. **ABORT FIX (test-infra): un-mocking ran real `ZmqCommandWorker` QThreads whose GC-before-join aborts the process when all 5 share a run (QThread-destroyed-while-running; cumulative, only at 5-together — bisected, confirmed I introduced it; orig-5 + any-4-of-5 clean). Added `tests/gui/shell/views/conftest.py` autouse stub replacing ZmqCommandWorker with a synchronous no-op (transport-only; rendered behavior still driven via direct `_on_*_loaded` slots; worker-creation tests override with their own patch). Whole views/ dir 171 pass, exit 0.** **Codex VERIFY amend cycle (gpt-5.5 high, in-batch): PASS no CRIT/HIGH; 2 MED amended — alarm-fetch assert made dispatch-order-independent (select by cmd name); valid-result band upgraded from ordering-only to exact log-space literals lower=[-4.5,-5.5,-6.5]/upper=[-3.5,-4.5,-5.5] + x positions (verified vs prod analytics_widgets.py:727-733).** Re-verified 92 pass×2 (incl all-5 abort re-check, exit 0) + ruff clean, src untouched. |
| 30 | 20 | 20 | 0 | 80 pass | tool-rail/top-bar + analytics views (persistent_context/replay_mode CLEAN, untouched). tool-rail active→rendered border-left QSS; top-bar clicks via real QTest.mouseClick on _ClickableLabel + rendered label text/color; deleteLater regression observed; chat-worker-leak via REAL AssistantChatPanel + _on_response() (killed the MagicMock-mirror tautology); analytics pressure/health rendered series+SeverityChip; cooldown-history exact X(parsed ts)/Y(duration) + full ys==[1..20]. Re-verified 80 pass + ruff, src untouched. |
| 29 | 50 | 48+1new | 0 | 97 pass | mw_v2↔panel wiring (9 files; overlay_container CLEAN). SYSTEMIC 30 HIGH: `_connected`-flag asserts → rendered control contract (`_*_btn.isEnabled()` / status label `.text()` / poll-timer `.isActive()`), monotonic frozen for stale/recent. reading-routing → rendered cell/curve/`_power_label.text()` / spy on_reading. f4_lazy_replay → real `_on_tool_clicked("analytics")` + `current_overlay=="analytics"` + exact replayed values. new_experiment_dialog → no-signal + exact error. **KEITHLEY SAFETY GAP CLOSED: new `test_smua_start_dispatches_exact_command_dict` asserts mw_v2 forwards the exact keithley_start command.** 3/3 teeth. Re-verified 97 pass×2 + ruff, src untouched. |
| 28 | 34 | 34 | 0 | 94 pass | operator-log/accent/exp-overlay/main_window_v2. (Started by the prior prod-fix session, left mid-edit with 5 failing + ruff; FINISHED here — all 5 were test-stub bugs, no prod gaps.) accent SOURCE-GREP→rendered/applied QSS + live palette; mw_v2 WIRING→OverlayContainer.currentWidget()/rendered panel; exp-overlay save/abort via real button+menu→exact experiment_update/experiment_abort command; wall-clock timestamps→frozen module clock. Re-verified 94 pass×2 + ruff, src untouched. |
| 27 | 26 | 26 | 0 | 102 pass | keithley(SAFETY)/kb/multiline. 12 keithley SAFETY-CONTROL tests now spy the dispatch seam + assert the EXACT command dict (keithley_start/stop/emergency_off/set_target/set_limits channel+payload), not the intermediate signal; cancel/off/fault assert NO command; A+B assert both per-channel commands; `_wait(400)` debounce sleeps eliminated (0 left). kb/multiline real list-select/button clicks + payloads. Teeth: wrong channel/on-off → FAIL. Re-verified 102 pass×2 + ruff, src untouched. |
| 26 | 31 | 31 | 0 | 98 pass | conductivity/cooldown-footer/instruments. ACTION-WEAK→real button clicks; MOCK-BYPASS→public on_reading()/_on_cooldown_status() result path; STATUS-WEAK→assert displayed status TEXT+color+counters. Re-verified 98 pass + ruff, src untouched. |
| 25 | 27 | 27 | 0 | 75 pass | archive+calibration panels. ACTION-WEAK → real button clicks: 12 `_export_*_btn.click()`, ZERO private `_on_export_*_clicked()` left (catches broken wiring); parquet worker lifecycle via click + fake-only-exporter; refresh asserts exact archive-list payload; export-thread event-gated (no sleep); calibration `_start_btn.click()` + exact experiment_start payload, JSON export sensor_id=="Т5", apply-policy emitting-fake-signal + ordered payload sequence, set_coverage([]) not private _bins. Re-verified 75 pass×2 + ruff, src untouched. |
| 24 | 22 | 21 | 1 | 60 pass | alarm-panel overlay (safety-adjacent). ACK tests now CLICK the rendered cellWidget button (col 7 v1 / col 5 v2) + assert exact dispatched command `{"cmd":"alarm_acknowledge","alarm_name":"hot"}` (proves button→command wiring); rendered table cells / SeverityChip muted+checkmark / disconnect keeps rows + disables ACK; exact summary "2 критических". **DEFERRED CRIT (item 11): test_reading_invalid_value_defaults_to_zero kept as-is with DEFERRED-NAN-11 marker — prod doesn't coerce NaN→0; needs architect.** Re-verified 60 pass + ruff, src untouched. |
| 23 | 34 | 34 | 0 | 62 pass | GUI widgets. WIDGET-CONTRACT-WEAK → rendered contract: phase_aware back/forward via real `_back_btn.click()` (catches disconnected wiring) + isEnabled() state; sensor_cell rename-escape via real QKeyEvent through eventFilter; phase_stepper pill accent/filled/future styling; temp/pressure plot curve getData() exact (x,y); quick_log exact 2 newest + truncation tooltip; time_window full ordered list; breadcrumb _overlay_label.text(); showcase asserts modal/breadcrumb/7 tiles/bento positions. No defers. Re-verified 62 pass + ruff, src untouched. |
| 22 | 14 | 10 | 4 | 63 pass | tools/dashboard. Fixed: sensor-grid guarded-pass (assert cell not None first + exact value), replay-session dry-run (publisher_socket raises-if-called), milestone exact "14ч 20мин"+labels, dynamic-grid per-cell refresh spies, experiment-card pill ACCENT/STATUS_OK/BORDER, mock-scenario monotone+exact values, eta/hero/milestone exact text. DEFERRED: 4 XSS-escaping tests — escapeHtml is CLIENT-SIDE JS (server.py:466, FastAPI returns raw JSON, browser escapes pre-innerHTML); needs Playwright/Selenium → ledger item 12. Re-verified 63 pass + ruff. |
