# VERIFY pass (amend cycle) — live log

PHASE C of the test sweep. After the FIX pass completed (batches 0-21, commit ad4a879),
each batch's now-strengthened test files get a READ-ONLY Codex (gpt-5.5 high) re-review
hunting ONLY for problems the FIX pass INTRODUCED: over-fit/brittle impl-detail asserts,
over-mocking that re-hides prod, residual false-confidence, tautologies, or a weakened
test. Real test-only problems → executor fix + independent re-run. Anything needing a
src/ change → DEFERRED (see TEST_SWEEP_STATUS.md ledger). Machine state:
`progress.json` `verify_next_batch`.

| Batch | Codex raised | test-only fixed | deferred | result | notes |
|------:|-------------:|----------------:|---------:|--------|-------|
| ⊕ | — | 1 | 0 | bonus | checkpoint gate (verify 5-9) surfaced a PRE-EXISTING time-of-day bug in tests/gui/shell/test_experiment_overlay.py::test_format_time_same_day_returns_hh_mm (unrelated to sweep): used `now - timedelta(hours=2)` which crosses midnight in the 00:00-02:00 window → "вчера HH:MM" (11) not "HH:MM" (5). Fixed with the repo's established noon-anchor pattern (matches sibling yesterday test). File 31/31. Rest of gate: 3247 pass, 0 other fail. |
| 09 | 3 | 3 | 0 | 69 pass | drivers lakeshore/visa + archive. F1/F2 calibration TAUTOLOGY (test computed expected via the same store.evaluate prod uses): built known Chebyshev curve (coeffs 15.0,2.0) → hand-computed literal 14.192, no store.evaluate in assert; F2 also asserts raw==82.98/SRDG/sensor_id (was wrong-raw blind). F3 reconfigure: keys-present → whole-dict =={7:1350,14:1700}. VISA spy/archive merge/KRDG/TCP CLEAN. Re-verified 69 pass + ruff. |
| 08 | 3 | 3 | 0 | 59 pass | drivers keithley/gpib/etalon. F1 gpib-no-IDN: patched the very _blocking_connect under test → now fake pyvisa RM, real path runs, assert clears>=1 + no *IDN?. F2 etalon first-cycle: timestamp-blind → assert Reading.timestamp==fromtimestamp(ts,UTC). F3 parquet burst: never checked cycle_ts → assert column equals seeded per-cycle ts. Keithley _last_v/source-off/force-off + disconnect-Event CLEAN. Re-verified 59 pass + ruff. |
| 07 | 11 | 5 | 6 | 52 pass | ZMQ bridge/subprocess/safety. FIXED: F4 serialization-error (object()→_TrulyUnserializable forces json fallback), F5 cancelled-error (best-effort contract, was non-falsifiable), F6 vacuum-guard (drop `or "мбар"`, assert channels+values), F8 launcher-poll (getsource→real _poll_bridge_data behavioral), F9 slow-commands (drop private _SLOW_COMMANDS, assert _timeout_for). DEFERRED (need src seam, ledger item 8): F7 inner-timeouts (engine wait_for), F1/F2/F10/F11 real-timeout flakes (35s REQ/5s heartbeat injectable), F3 overflow closure. CRITICAL timeout-inversion untouched (item 1). Re-verified 52 pass + ruff. |
| 06 | 2 | 2 | 0 | 35 pass | sensor-diag/sqlite/user-prefs. F1 wal-recovery-after-crash: real subprocess crash sim kept, added kill-on-timeout (p.is_alive→kill→join) to prevent pytest cleanup hang; 3/3 no flake. F2 history-max-limit: private _add_to_history → public save_last_experiment(operator=..) + get_history + ==20 exact order. alarm-clears + suggest-name CLEAN. Re-verified 35 pass + ruff. |
| 05 | 5 | 4 | 0 | 77 pass | SAFETY-CRITICAL. F1 rate-ignores-non-temp: critical_channels=[] made no-fault trivial → voltage now CRITICAL + 65 high-slope V samples + assert RUNNING (unit-level exclusion). F2 run-permitted-monitored: weak `or "Устаревшие"` → assert FAULT_LATCHED + channel name "Т1 Криостат верх" in fault_reason. **F3 time-filter: exact-50 fix EXPOSED A REAL FLAKE — sub-µs base_ts=time.time() made row-50 stored-ts jitter across boundary (49/50); fixed by whole-second anchor float(int(time.time())-7200); stress-verified 0/20.** F5 sequential-connect: fixed 0.3s outer wait → d1/d2 Events+wait_for. F4 min_points=60 KEPT BY-DESIGN (two-sided contract, not over-fit). Re-verified 77 pass + ruff. |
| 04 | 7 | 7 | 0 | 54 pass | core interlock/mem-leaks/p0/persistence. F1/F2 maxlen over-fit → behavioral buffer_size()/len<=maxlen. F3 broadcast_pump: ran LOCAL reimpl → now real web.server._broadcast_pump. F4 alarm-count: `0.0 in values` trivially true → deadline-poll activation then final 0.0. F5 safety-publish: `await_count>=1` tautology → baseline-after-start + wait READY + count increased. **F6 action-async: FIRST FIX WRONG (2nd reading same channel, TRIPPED-latch hid await-vs-fire-forget); REDONE with 2 conditions T1/T2 + raises(TimeoutError) negative-assert — independent review caught it.** F7 load_config: private _interlocks read → behavioral trip/cooldown. persistence-ordering ⭐ CLEAN. Re-verified 54 pass + ruff. |
| 03 | 4 | 4 | 0 | 43 pass | core experiment/photos. F1 sidecars-atomic: asserted only "some .json" → now asserts composition/ photo-sidecar path + metadata.json by name. F2 wal-verification: private _get_connection() → public start_experiment() with SQL-inspecting fake (keys on "delete" not "wal"). F3 html-escape: removed `or "script" not in text` escape-hatch → assert exact escaped fragments present + raw absent. F4 restores_max_height: added QT_QPA_PLATFORM=offscreen guard (fix introduced QApplication w/o headless guard). Re-verified 43 pass + ruff. |
| 02 | 3 | 3 | 0 | 48 pass | core cooldown/engine/event. F1 force_kill: patched _is_pid_alive but never asserted it consulted → assert_called_once_with(12345). F2 cooldown latch-fault: `args[1] is not None` → assert event.level=="CRITICAL"+alarm_id. F3 leak-rate duration override: brittle `_window_override==120` (which the FIND finding itself suggested) → observable should_finalize() False@61s/True@121s. leak-rate copied-_dispatch HIGH stays deferred. Re-verified 48 pass + ruff. |
| 01 | 4 | 4 | 0 | 54 pass | core storage/calib/channel. F1/F2 audit_fixes: OVERRANGE +inf / UNDERRANGE -inf tests selected value but never asserted it → now assert isinf+sign. F3 calib export/import: `evaluated_t>0.0` → assert imported vs original eval agree <1e-9 (real round-trip). F4 curve list/lookup: single curve hid channel_key matching → added 2nd curve on LS218:CH2 + assert lookup returns it. All test-only, no defer. Re-verified 54 pass + ruff. |
| 00 | 5 | 3 | 2 | 68 pass | alarm core. F1 event_history_bounded: split 500/500 was eviction-invariant → now per-cycle activate value (60.0+i*0.01) + assert retained min-value from late cycle; teeth-checked (FAIL@400 / PASS@600). F5 legacy_cleanup: now walks nested composite `conditions` not just top-level check. F2 sustained: dropped redundant `_sustained_since` private assert (behavioral asserts kept). DEFERRED F3/F4 (status_shape tautology + ack): real path is `_handle_gui_command` engine closure (engine.py:2347/2392) — needs extraction, like batch02/18. Independently re-verified 68 pass + ruff-clean |
