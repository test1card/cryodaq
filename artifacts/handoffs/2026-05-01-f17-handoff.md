# F17 overnight 2026-05-01 — architect review

## Branch

`feat/overnight-f17-cold-rotation` at `0435121`

## Implementation

Files changed:
- `src/cryodaq/storage/cold_rotation.py` — 446 LOC (new)
- `src/cryodaq/storage/archive_reader.py` — 175 LOC (new)
- `config/housekeeping.yaml` — +10 LOC (cold_rotation section)
- `tests/storage/test_cold_rotation.py` — 10 tests (new)
- `tests/storage/test_archive_reader.py` — 6 tests (new)

LOC: +640 / 0

Tests: 16 total (10 rotation + 6 reader)

## Acceptance criteria

1. [PASS] ColdRotationService rotates SQLite files older than age_days to Parquet with Zstd compression
2. [PASS] Today's active write file is never rotated
3. [PASS] Already-rotated files skipped (index.json check)
4. [PASS] Archive index.json updated atomically after each rotation
5. [PASS] Row count verified before SQLite deletion
6. [PASS] SQLite + WAL + SHM sidecars deleted after successful rotation
7. [PASS] ArchiveReader.query() spans SQLite and Parquet transparently
8. [PASS] Day-by-day iteration uses UTC dates (no local-timezone drift at midnight)
9. [PASS] archive_dir/year=YYYY/month=MM/<stem>.parquet layout
10. [PASS] ColdRotationService daemon: start/stop/run_once API

## Audit history

**Codex (gpt-5.5, high reasoning):** CONDITIONAL → CONDITIONAL → PASS (3 passes)

Cycle 1 findings and fixes:
- CRITICAL: `_read_index()` silently returned `{"files": []}` on corrupt JSON, causing `_update_index()` to overwrite entire index. → Fixed: raises RuntimeError; `_update_index()` call wrapped in try/except that cleans up Parquet and returns None.
- HIGH: `archive_reader._load_index()` swallowed corrupt index.json → silently dropped rotated-day data. → Fixed: raises RuntimeError.
- HIGH: epoch computation before UTC normalization — naive datetime inconsistency. → Fixed: epochs computed from `from_utc.timestamp()` / `to_utc.timestamp()`.
- MEDIUM: No asyncio.Lock for concurrent `run_once()` calls. → Fixed: `self._lock = asyncio.Lock()` in `__init__`; `run_once()` wrapped in `async with self._lock:`.
- Additionally: missing `UTC` import in `archive_reader.py` — fixed.

Cycle 2 findings: none blocking. PASS.

## Test baseline

16 passed (cold_rotation: 10, archive_reader: 6)

## Risk

Medium. Rotation is one-way (SQLite deleted after Parquet written + verified).
Safeguards: row count verification before delete; partial Parquet cleaned up on any failure; corrupt index aborts the rotation pass rather than overwriting.

## ARCHITECT DECISION NEEDED

None. All parameters specified in ROADMAP F17.

**Note:** `ArchiveReader` is a standalone read layer. Wiring it into the engine's existing replay service (`storage/replay.py`) is deferred — not required for F17 acceptance.
