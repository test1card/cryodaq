"""F32 — RAG indexer foundation.

Standalone semantic search over the experiment archive (metadata.json,
F31 vault notes, operator log entries). Stage 1 ships the indexer +
searcher + CLI; Stage 2 (AssistantQueryAgent integration) is a separate
spec.
"""
